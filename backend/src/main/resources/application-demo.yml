spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:postgres}:5432/${POSTGRES_DB:ebis_demo}
    username: ${POSTGRES_USER:ebis_demo}
    password: ${POSTGRES_PASSWORD}

logging:
  level:
    com.ebis.nacionalidad: INFO

management:
  endpoint:
    health:
      show-details: never
