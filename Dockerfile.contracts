FROM node:24-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY hardhat.config.ts tsconfig.json ./
COPY contracts ./contracts
COPY scripts ./scripts

CMD ["sh", "-c", "npm run build && BESU_DEPLOYER_PRIVATE_KEY=$(node scripts/print-dev-private-key.js 0) npm run deploy"]
